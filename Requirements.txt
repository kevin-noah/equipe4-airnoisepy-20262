# Dépendances d'exécution d'AirNoisePy.
#
# Ce fichier reflète à l'identique la section [project].dependencies de
# pyproject.toml. La méthode d'installation recommandée reste « pip install . »
# (qui lit pyproject.toml) ; ce fichier sert d'alternative pour
# « pip install -r Requirements.txt ».
#
# contextily et rasterio NE sont PAS listés ici : ce sont des dépendances
# optionnelles (imports gardés par try/except) pour la correction d'altitude
# AGL et le fond de carte. La bibliothèque et les tests fonctionnent sans elles.

numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
folium>=0.14
requests>=2.28
openpyxl>=3.1
imageio>=2.34
streamlit>=1.4
streamlit_folium>=0.24
plotly>=6.7
